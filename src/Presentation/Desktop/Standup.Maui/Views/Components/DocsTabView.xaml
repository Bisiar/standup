<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Standup.Application.ViewModels;assembly=Standup.Application"
             xmlns:models="clr-namespace:Standup.Application.Models;assembly=Standup.Application"
             x:Class="Standup.Maui.Views.Components.DocsTabView"
             x:DataType="viewModels:DocumentsViewModel">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Document List -->
        <Border Grid.Column="0" Padding="16"
                BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">
            <VerticalStackLayout Spacing="12">
                <!-- Header -->
                <VerticalStackLayout Spacing="4">
                    <Label Text="Documentation" FontSize="20" FontAttributes="Bold" />
                    <Label Text="User guides and help" FontSize="12" TextColor="Gray" />
                </VerticalStackLayout>

                <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#E0E0E0, Dark=#333333}" />

                <CollectionView ItemsSource="{Binding Documents}"
                                SelectedItem="{Binding SelectedDocument}"
                                SelectionMode="Single">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:DocumentItem">
                            <Border Margin="0,4" Padding="12,10"
                                    StrokeShape="RoundRectangle 8"
                                    Stroke="Transparent"
                                    BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#2D2D2D}">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:DocumentsViewModel}}, Path=SelectDocumentCommand}"
                                        CommandParameter="{Binding .}" />
                                </Border.GestureRecognizers>

                                <VerticalStackLayout Spacing="2">
                                    <Label Text="{Binding Title}" FontSize="14" FontAttributes="Bold" />
                                    <Label Text="{Binding Category}" FontSize="11" TextColor="Gray" />
                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </Border>

        <!-- Document Content rendered as HTML -->
        <WebView Grid.Column="1"
                 x:Name="MarkdownWebView"
                 BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1A1A1A}" />

        <!-- Loading Indicator -->
        <ActivityIndicator IsRunning="{Binding IsLoading}"
                          IsVisible="{Binding IsLoading}"
                          HorizontalOptions="Center"
                          VerticalOptions="Center"
                          Grid.ColumnSpan="2" />
    </Grid>
</ContentView>
